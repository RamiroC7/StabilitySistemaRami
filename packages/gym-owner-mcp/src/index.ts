// Placeholder de T0 (specs/gym-owner-mcp-vercel/tasks.md).
//
// El server MCP remoto real (mcp-handler, tool `query_gym_data`,
// Authorization Server OAuth 2.1, etc.) se arma en las tasks siguientes
// de ese spec (Fase B en adelante). Este archivo solo existe para que el
// paquete sea un módulo TypeScript válido y `tsc`/`tsc --noEmit` tengan algo
// que compilar.
export {};
